.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
.terraformrc
terraform.rc

# Config generated by `tofu plan -generate-config-out=` — review, fold into the
# real .tf files, then delete. Never commit it raw.
generated.tf
